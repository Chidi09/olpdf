# Test and development dependencies — not installed in production
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
httpx>=0.27          # async test client for FastAPI
anyio[trio]          # required by pytest-asyncio
