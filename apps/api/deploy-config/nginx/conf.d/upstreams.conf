upstream olpdf_api_upstream {
    server olpdf-api-blue:8000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# Export service — Go PDF generator, always at olpdf-export:8002 on the shared network
# Called directly by the API container, not proxied through nginx

